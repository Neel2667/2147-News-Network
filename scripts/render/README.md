# Automated Renderer

This folder contains an optional Playwright + FFmpeg renderer.

Manual OBS recording is still recommended for the first pilot quality pass.

Use automation only after the HTML scenes are visually approved.
